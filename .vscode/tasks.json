{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Clean All",
            "type": "shell",
            "command": "rm -r build",
            "detail": "Nukes the entire build folder.",
            "presentation": {
                "reveal": "always"
            }
        },
        {
            "label": "Build with MinGW Compiler",
            "type": "shell",
            "command": "cmake -G 'MinGW Makefiles' -B build",
            "detail": "Configures the project using MinGW compiler, setting up all toolchains, options, and build files.",
            "presentation": {
                "reveal": "always"
            }
        },
        {
            "label": "Build",
            "type": "shell",
            "command": "cmake --build build",
            "detail": "Builds (compiles) the project based on the previously generated configuration in the build folder.",
            "presentation": {
                "reveal": "always"
            }
        },
        {
            "label": "Build All",
            "dependsOn": [
                "Build with MinGW Compiler",
                "Build"
            ],
            "dependsOrder": "sequence",
            "detail": "Configures and builds the project using the MinGW compiler.",
            "presentation": {
                "reveal": "always"
            }
        }
    ]
}
